apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: dev-tms-oppgaver-mikrofrontend
  namespace: min-side
  labels:
    team: min-side
spec:
  envFrom:
    - secret: dev-tms-oppgaver-mikrofrontend-secrets
  image: {{ image }}
  port: 7100
  liveness:
    path: /dev-tms-oppgaver-mikrofrontend/internal/isAlive
    initialDelay: 10
  readiness:
    path: /dev-tms-oppgaver-mikrofrontend/internal/isReady
    initialDelay: 10
  prometheus:
    enabled: true
    path: /dev-tms-oppgaver-mikrofrontend/internal/metrics
  replicas:
    min: 2
    max: 4
  ingresses:
    - "https://person.nav.no/dev-tms-oppgaver-mikrofrontend"
  resources:
    limits:
      cpu: "3"
      memory: 768Mi
    requests:
      cpu: "500m"
      memory: 512Mi
